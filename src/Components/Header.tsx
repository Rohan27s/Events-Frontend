import React from 'react'

type Props = {}

const Header = (props: Props) => {
  return (
    <header className="header">
        <span>
            <h2>EVENTS</h2>
        </span>
    </header>
  )
}

export default Header